{
    "version": 2,
    "builds": [
        {
            "src": "npx prisma migrate dev --name init",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(*.)",
            "dest":"/"
        }
    ]
}